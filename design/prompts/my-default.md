# My Default Reference Prompt

The attached Home image is the approved GoodsPocket visual style anchor.

Create exactly one `393:852` My screen with this hierarchy: iOS status bar, centered `마이` title, compact local-profile card, `내 요약` title, one 2x2 summary card, `계정 관리` title, one three-row management card, and the shared bottom navigation with centered orange plus button.

Required copy:

- Profile: `로컬 프로필 / 연결되지 않음 / 계정을 연결하지 않아도 로컬로 계속 사용할 수 있어요.`
- Summary: `보유 굿즈 98 / 활성 예약 11 / 이번 달 지출 243,600원 / 임박 일정 3`
- Management: `동기화 및 백업 / 나중에 계정 연결과 백업을 설정할 수 있어요.`, `알림 / 알림 꺼짐`, `설정 / 앱 설정을 관리해요.`
- Only Settings shows a separate thin chevron; My is selected in the shared bottom navigation.

Support dynamic profile/status/summary values, Settings navigation, quick add, primary navigation, and vertical scrolling. Sync/backup and Notifications remain informational. Do not add profile editing, avatar upload, login, account-connect CTA, interested goods, recent activity, social statistics, logout, toggles, commerce actions, notification bell, or a screen-local FAB.

Use the Home visual system: `#FFFCF8` background, `#FF7445` orange, `#36C781` green, `#8F6EF2` purple, `#202838` strong text, `#8A8F9B` muted text, compact Korean sans typography, warm off-white cards, `#EFEDEC` outlines, 10-12px radii, almost flat shadows, and the shared bottom-navigation geometry.

Use a neutral gray circular profile placeholder with no initials or text. Output one screen only with no phone frame, board, collage, annotations, outer background, explanation, decorative illustration, or unsupported action.

Final correction: remove the generated top-right notification bell and dot while preserving the remaining structure. The implementation replaces the generated avatar shading with a flat `#D7D2CC` Compose placeholder.
