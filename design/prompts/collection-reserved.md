# Collection Reserved Reference Prompt

Create a second full-screen GoodsPocket Collection reference image for the Reserved selected state.

Use the Collection Owned image generated earlier in the same GPT Web conversation as the shared visual source. Keep the Collection hierarchy unchanged:

1. iOS status bar
2. centered `컬렉션` title
3. `보유품 / 예약중 / 전체` segmented control
4. full-width `이름 또는 시리즈` search field
5. result count
6. three-column goods grid
7. fixed collection summary band
8. shared bottom navigation with centered orange plus button

Change only the state-specific content:

- select `예약중` in orange; keep `보유품` and `전체` inactive
- result count: `총 11개`
- every card uses an orange/peach `예약중` badge
- card metadata shows reservation store and release date
- summary remains `보유품 98`, `예약중 11`, `총 구매액 ₩243,600`
- Collection remains the selected bottom-navigation destination

Use these exact sample card contents:

- `니지산지 애니버서리 배지 세트` / `애니플러스 샵 · 발매 2026.03.28` / `예약중`
- `프로젝트 세카이 한정 태피스트리` / `멜론북스 · 발매 2026.04.11` / `예약중`
- `루피 기어5 피규어` / `예약 스토어 · 발매 2026.05.20` / `예약중`
- `한정 아크릴 스탠드` / `애니메이트 · 발매 2026.06.08` / `예약중`
- `애니버서리 키링 세트` / `공식 스토어 · 발매 2026.06.21` / `예약중`
- `콜라보 인형` / `팝업 스토어 · 발매 2026.07.03` / `예약중`

Continue using uniform neutral gray media placeholders with no initials, letters, fake artwork, or pixel art. Do not add favorite, wishlist, sale, sort, filter-menu, view-mode, or overflow actions.

Keep the GoodsPocket visual system from the attached Home anchor:

- warm `#FFFCF8` background
- `#FF7445` primary orange
- `#36C781` owned/success green
- `#8F6EF2` secondary/event purple
- `#202838` strong text and `#8A8F9B` muted text
- compact Korean sans typography with zero letter spacing
- warm `#FEFBF8` cards, subtle `#EFEDEC` outlines, 8px card radius, and almost flat shadows

Output exactly one final `393:852` UI image. Do not include a phone frame, collage, annotations, explanation, or outer background.
